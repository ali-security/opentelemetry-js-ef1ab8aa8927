on:
  pull_request_target:
    branches:
      - main

jobs:
  label-release:
    # disabled on the seal fork: not needed for cross-platform gate
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo this is a release PR
      - run: gh pr edit ${{ github.event.pull_request.number }} --add-label release
